{
  "name": "@velo-sci/notebook-plugin-export",
  "version": "0.6.3",
  "description": "PDF/DOCX export plugin for sci-notebook",
  "publishConfig": {
    "access": "public"
  },
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "dev": "tsup src/index.ts --format esm --watch --dts",
    "test": "vitest run"
  },
  "dependencies": {
    "@velo-sci/notebook-core": "workspace:*",
    "html2canvas": "^1.4.1",
    "jspdf": "^4.1.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "vitest": "^1.0.0"
  }
}
